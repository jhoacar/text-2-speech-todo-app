import { BottomNavigation, Typography } from '@mui/material';

function Footer() {
  return (
    <BottomNavigation sx={{ padding: '1rem' }}>
      <Typography color="InfoText">
        Footer
      </Typography>
    </BottomNavigation>
  );
}

export default Footer;
